# HEI Tailwind v4.1

Ce projet utilise HTML5 et Tailwind CSS v4.1.

## 🚀 Installation

Une fois le projet cloné, installe les dépendances :
`npm install`

## 🛠️ Développement

Pour lancer le compilateur Tailwind en mode "watch" (il mettra à jour le CSS à chaque modification) :
`npm run dev`

## 📦 Build final
Pour générer un CSS compressé pour la mise en ligne :
`npm run build`